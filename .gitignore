# Node.js dependencies
# Ignoring the node_modules folder where dependencies are installed.
# Ignorar la carpeta node_modules donde se instalan las dependencias.
node_modules/

# Logs
# Ignore log files, useful for debugging but not necessary in the repository.
# Ignorar archivos de registro, útiles para depuración pero no necesarios en el repositorio.
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
# Ignore .env files that store sensitive information like API keys or database credentials.
# Ignorar los archivos .env que contienen información sensible como claves API o credenciales de base de datos.
.env
.env.local
.env.*.local

# OS-specific files
# Ignore operating system-specific files like macOS Finder files or Windows system files.
# Ignorar archivos específicos del sistema operativo como los de Finder en macOS o del sistema en Windows.
.DS_Store
Thumbs.db

# IDE and editor files
# Ignore files and folders generated by IDEs or text editors (e.g., Visual Studio Code, IntelliJ, etc.).
# Ignorar archivos y carpetas generados por IDEs o editores de texto (p. ej., Visual Studio Code, IntelliJ, etc.).
.vscode/
.idea/
*.swp
*.sublime-workspace
*.sublime-project

# Temporary files
# Ignore temporary files generated during development or by build processes.
# Ignorar archivos temporales generados durante el desarrollo o por procesos de compilación.
*.tmp
*.temp
*.bak
*.old
*.pid
*.seed
*.pid.lock

# Build files
# Ignore files and folders generated during the build process.
# Ignorar archivos y carpetas generados durante el proceso de compilación.
dist/
build/
coverage/

# Debugging snapshots
# Ignore snapshots created by Jest or other testing tools.
# Ignorar capturas creadas por Jest u otras herramientas de pruebas.
*.snap

# Deployment files
# Ignore files related to deployment tools (e.g., PM2 or Heroku).
# Ignorar archivos relacionados con herramientas de despliegue (p. ej., PM2 o Heroku).
.pm2/
*.tar.gz